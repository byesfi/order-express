module entrypoint {
  requires spring.web;
  requires org.hibernate.orm.core;
  requires spring.tx;
  requires domain;
  requires java.validation;
  requires spring.context;
  requires spring.beans;
  requires org.slf4j;
}